/*
 * <%= name %>
 * <%= description %>
 * <%= sketch.title %>
 * <%= sketch.description %>
 * <%= author %>
 * scripts/<%= sketch.name %>.js
 */
(function (lib) {
  var $el = $('#<%= sketch.name %>');

})(window.lib || (window.lib = {}));
